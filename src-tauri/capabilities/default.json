{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The overlay is decorative and must not be able to do anything but listen for cues. It gets events only — no window, path, or shell access. Settings and onboarding additionally need to call the app's own commands, which are allowed by default and do not require a permission here.",
  "windows": ["overlay-*", "settings", "onboarding"],
  "permissions": ["core:event:default"]
}
